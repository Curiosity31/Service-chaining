spring:
  application:
    name: serviceA

server:
  port: 0

serviceBUrl: http://serviceB/serviceB

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/
      #defaultZone: http://localhost:8761/eureka/


management:
  endpoints:
    web:
      exposure:
        include: "*"