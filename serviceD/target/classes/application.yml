spring:
  application:
    name: serviceD

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/


management:
  endpoints:
    web:
      exposure:
        include: "*"